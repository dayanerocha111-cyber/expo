import React, { useState } from 'react';
import { Text, View, ScrollView, StyleSheet, Image, TouchableOpacity } from 'react-native';

export default function App() {
  const [screen, setScreen] = useState('home');

  if (screen === 'home') return <Home setScreen={setScreen} />;
  if (screen === 'dicas') return <Dicas setScreen={setScreen} />;
  if (screen === 'cultura') return <Cultura setScreen={setScreen} />;
  if (screen === 'quiz') return <Quiz setScreen={setScreen} />;

  return null;
}

function Menu({ setScreen }) {
  return (
    <View style={styles.menu}>
      <TouchableOpacity onPress={() => setScreen('home')}><Text style={styles.menuItem}>🏠 Início</Text></TouchableOpacity>
      <TouchableOpacity onPress={() => setScreen('dicas')}><Text style={styles.menuItem}>🌱 Dicas</Text></TouchableOpacity>
      <TouchableOpacity onPress={() => setScreen('cultura')}><Text style={styles.menuItem}>🎉 Cultura</Text></TouchableOpacity>
      <TouchableOpacity onPress={() => setScreen('quiz')}><Text style={styles.menuItem}>🎯 Quiz</Text></TouchableOpacity>
    </View>
  );
}

function Home({ setScreen }) {
  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>Projeto Agrinho 2026</Text>
      <Text style={styles.subtitle}>Escola Delfina Teixeira de Castro</Text>

      <Image
        source={{ uri: 'https://images.unsplash.com/photo-1600431521340-491eca880813' }}
        style={styles.image}
      />

      <Text style={styles.text}>
        Viver Bem no Semiárido - Município de Caridade
      </Text>

      <Text style={styles.highlight}>
        Conheça Aninha e João do Sertão 🌵
      </Text>

      <Image
        source={{ uri: 'https://images.unsplash.com/photo-1601050690597-df0568f70950' }}
        style={styles.image}
      />

      <Text style={styles.text}>
        Um aplicativo feito para valorizar nossa cultura, nossa gente e ensinar como viver bem no semiárido.
      </Text>

      <Menu setScreen={setScreen} />

      <Text style={styles.footer}>
        Criado por: Coordenadora Pedagógica Daiane Vieira
      </Text>
    </ScrollView>
  );
}

function Dicas({ setScreen }) {
  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>Dicas do Semiárido</Text>

      <Text style={styles.topic}>💧 Economia de Água</Text>
      <Text style={styles.text}>Reutilizar água, usar cisternas e evitar desperdício</Text>

      <Text style={styles.topic}>🌱 Agricultura Familiar</Text>
      <Text style={styles.text}>Valorizar a feira local e plantar alimentos resistentes</Text>

      <Image
        source={{ uri: 'https://images.unsplash.com/photo-1598514983318-2f64f8f4796c' }}
        style={styles.image}
      />

      <Text style={styles.topic}>🧶 Artesanato (Crochê)</Text>
      <Text style={styles.text}>O crochê gera renda e valoriza a cultura local</Text>

      <Text style={styles.topic}>🌞 Proteção ao Sol</Text>
      <Text style={styles.text}>Evitar horários quentes e usar proteção</Text>

      <Menu setScreen={setScreen} />
    </ScrollView>
  );
}

function Cultura({ setScreen }) {
  return (
    <ScrollView style={styles.container}>
      <Text style={styles.title}>Cultura de Caridade</Text>

      <Text style={styles.topic}>🎉 Festa de Santo Antônio</Text>
      <Image
        source={{ uri: 'https://images.unsplash.com/photo-1590080877777-8b5c3e
